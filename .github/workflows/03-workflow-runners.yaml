name: 03 - Workflow Runners
on:
  push:

jobs:
  os-echo:
    runs-on: windows-latest
    steps:
      - name: Show OS
        run: |
          echo "This job is running on an ${{ runner.os }} runner."
      
