name: 01 - Building Blocks
on:
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world!"
  goodbye-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "goodbye world"
